from rotator.views.pixel import pixel_form, pixel_one_view, pixel_form_edit
from rotator.views.views import sites_list, sites_form, sites_form_edit, sites_one_view

__author__ = 'igorzygin'



from django.conf.urls import patterns, url


urlpatterns = patterns('/',
    # url(r'^$', index),
    # url(r'^sites/create/', sites_form),
    # url(r'^sites/edit/(?P<id>[0-9]+)/', sites_form_edit),
    # url(r'^sites/(?P<site_id>[0-9]+)/create_pixel/', pixel_form),
    # url(r'^sites/(?P<id>[0-9]+)/', sites_one_view),
    # url(r'^pixel/(?P<id>[0-9]+)/edit', pixel_form_edit),
    # url(r'^pixel/(?P<id>[0-9]+)/', pixel_one_view),
    # url(r'^sites/', sites_list),



)
